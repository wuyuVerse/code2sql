## 对话摘要

1. 讨论 `extract_keyword_data` 新增 use_llm 功能需求：当 use_llm=True 时，使用 LLM 判断记录是否与17个GORM特殊关键字相关，并将匹配记录提取、删除并保存，提示/关键词列表放入 config/data_clean/special_keyword_prompt.py。

2. 需遵循高级工程师任务执行规则：先明确范围和计划，等待用户确认后再修改代码。

当前：助手确认需求并输出实现计划，等待用户授权。
## 实现完成摘要

✅ **任务状态：已完成**

### 主要修改

1. **配置文件** - 创建了 `config/data_clean/special_keyword_prompt.py`，包含17个特殊GORM关键词及详细的LLM提示模板

2. **核心功能** - 修改了 `WorkflowManager.extract_keyword_data` 方法，新增 `use_llm: bool = False` 参数
   - 当 use_llm=False 时保持原有正则匹配逻辑
   - 当 use_llm=True 时调用新增的 `_extract_keyword_data_with_llm` 方法
3. **LLM集成** - 使用异步并发调用LLM，每条记录独立分析，支持并发控制(semaphore=5)
   - 解析LLM响应，支持JSON列表格式和逗号分隔格式
   - 验证关键词有效性，只保留预定义列表中的关键词
4. **数据保存** - 保存匹配记录到 llm_keyword_matched_records.json
   - 保存未匹配记录到 llm_keyword_unmatched_records.json
   - 生成详细统计信息到 llm_keyword_statistics.json

### 测试验证
- 创建了 `test_llm_keyword_extraction.py` 测试脚本
- 包含3个测试用例：Preload问题、Transaction问题、普通查询

### 技术实现要点
- 复用现有LLM客户端和异步调用机制(call_async)
- 严格按照最小变更原则，仅修改必要文件
